---
# Дефолты роли — переопределяются из group_vars / host_vars / inventory

agent_listen_port:  8443
agent_listen_ip:    "127.0.0.1"

agent_max_path_depth: 5
agent_required_eku:   "1.3.6.1.5.5.7.3.2"

agent_allowed_paths:  []
agent_denied_paths:   []
agent_protected_groups: []

agent_allow_expired_cert_in_test: false

# Thumbprint листового сертификата NC (опционально)
agent_client_cert_thumbprint: ""
